# Candidates-v2 — the "AdamW killer (LoRA quality × gap × time)" hunt — results

> Branch: `integration/candidates-v2` (OFF main — for review). Generated by the crusade of
> 2026-06-07. **11 published optimizers** implemented on the kaon backend (9 standalone + 2
> wrappers), tuned with our proxy/profiler/battery infra, and judged against the field. **This is
> the review doc; the live anchor with the blow-by-blow is `tmp/CANDIDATES_V2_PLAN.md` (gitignored).**

> ## ⭐ Round-2 headline (the gap-frontier wrappers over Adakaon)
> The deep-research's top two picks for *moving* the generalization frontier (not just sliding along
> it) — both implemented as wrappers over the Adakaon factored backend:
> - **SAM (sharpness-aware, 2-pass)** — the campaign's most important scientific result: on the proxy
>   it **lowers the train-val GAP 14–31 % vs a matched non-SAM Adakaon** (monotonic in rho, 2 seeds),
>   via a *real* flat-minima effect (raises train loss more than test) — NOT underfitting. At full
>   battery scale: gap **+0.0163 vs Adakaon-bf16's +0.0204 (−20 %)**, confirmed. Cost: honest ~2×
>   per step (24 ms). It tightens the gap at a small loss cost — needs more steps/a schedule to turn
>   that into an absolute quality win, and a real FID test to confirm the perceptual payoff.
> - **Lookahead (k-step slow-weight averaging)** — gets the **lowest held-out loss of ALL candidates
>   (0.0718)**, beating even Adakaon-bf16 (0.0733), at Adakaon speed; mid gap (+0.0188). Weight
>   averaging is a cheap, real loss win. Memory ~4 B/p (Adakaon + the φ buffer).
>
> Both are wrappers → they pin down the campaign's **#1 backend extraction**: a shared codec
> full-state buffer (ScheduleFree `z` ≡ Lookahead `φ`) + a WrapsOptimizer/SlowWeights train-eval
> mixin. See "Backend dividend" below.
>
> A measurement fix shipped with them: `opt_state_bytes_per_param` now traverses a wrapper's inner/
> base optimizer (it previously under-counted wrappers — Lookahead read 2.00 not ~4.06, SAM 0.00 not ~2.06).

> ✅ **Battery fix applied (2026-06-07).** The convergence target was "the worst optimizer's best
> loss", which let underfitting optimizers inflate the bar and distort the convergence/time×quality
> dimension. It is now the **field's MEDIAN best-loss** (`battery.py`), so a few underfitters can't
> move it — "steps/time to the field's median final quality". The 17-optimizer table below is the
> clean, consistent global run (all non-frozen optimizers re-measured fresh at best config).

## ✅ Decision (locked 2026-06-07)
**Kept** (stay in the live registry, on `integration/candidates-v2`): **ADOPT, ScheduleFree, Lookahead,
SAM, AdamP, AdaBelief** (the wrappers run over Adakaon). **Discarded** (code preserved on branch
`candidates-v2-discarded`; numbers archived in `results_archive.json` so the charts/README keep them):
**Adan, MARS, Grams, Adai, AdEMAMix**. Rationale per the strengths/weaknesses below — in short: Adan is
Pareto-dominated once memory+speed count; MARS is heavy with a mid gap; Grams memorizes; Adai is the
heaviest + slowest-on-LoRA + the only perceptual failure; AdEMAMix is mid here but earns one last
*long-run* retest before final burial. AdamP was upgraded from "conditional" to keep (it's #3 loss /
#5 gap / light; only slow, and its norm-layer edge is untested on the proxy).

## TL;DR
- **11 candidates** — 9 standalone (AdaBelief, MARS, AdEMAMix, Adan, ScheduleFree, ADOPT, Grams, AdamP, Adai) + 2 wrappers (Lookahead, SAM) — built on the shared backend (codec momentum + factored quantized 2nd moment +
  bit-exact foreach + cautious + GC + dtype-safe checkpoint), each with **bit-exact foreach↔per-param
  parity** across {fp32, bf16, int8, 4bit} and a reference-correctness test. ~210 new tests, all green.
- **Two candidates win a rank-1 axis** in the 13-optimizer control battery (C=128, N=2000, 2-seed):
  - **ScheduleFree — 🥇 convergence / time-to-quality** (fastest of all 13 to the quality bar, beating
    fused AdamW), at **2.03 B/param**. *5th overall.* The clear keeper.
  - **ADOPT — 🥇 generalization (lowest train–val gap, +0.0070)** — it **dethroned AdaPNM** on the gap,
    light (2.03 B/p) and the fastest LoRA-step of the candidates. *6th overall.* Caveat: it reaches that
    gap by **underfitting** (highest loss) — which, under our memorization-resistance thesis, is a
    feature worth a real FID test, not a bug.
- The others land mid-pack and are heavier; none beats our in-house **Lion / Adakaon / AdaPNM** on the
  mean. AdaPNM remains the constant-LR champion.
- **The biggest prize is the backend dividend** (below): 7 independent implementations converged on the
  same handful of consolidations and one genuinely new reusable primitive (a schedule-free mixin).

## The field (13 optimizers, each at its best config)
Reference `torch.AdamW (fused)`; in-house `Adakaon-nomom/-bf16`, `Lion`, `AdaPNM`, `AdaMuon`;
candidates `AdaBelief` (+`-b999`), `MARS`, `AdEMAMix`, `Adan`, `ScheduleFree`, `ADOPT`.
Tuned on the C96/N800 proxy (ranked by held-out loss); the battery runs them at C128/N2000/2-seed
on the REX + progressive-resolution recipe. **Lower is better everywhere.** Full tables:
[`RANKINGS.md`](RANKINGS.md); cache: [`results.json`](results.json).

### Overall — clean global run, field of 17 (median convergence target). Mean rank is a weak picker; read the 🥇 wins.
| # | optimizer | mean rank | 🥇 rank-1 | family |
|---|---|---|---|---|
| 1 | Lion | 4.6 | — | in-house |
| 2 | Adakaon-nomom | 5.9 | 🥇 memory | in-house |
| 3 | **ADOPT** | 6.9 | **🥇 generalization** | candidate |
| 4 | Adakaon-bf16 | 7.1 | 🥇 convergence | in-house |
| 5 | torch.AdamW (fused) | 7.4 | 🥇 iter/LoRA-speed | reference |
| 6 | **AdaBelief** | 7.6 | — | candidate |
| 7 | **AdamP** | 8.3 | — | candidate |
| 8 | AdaPNM | 8.4 | 🥇 constant-LR | in-house |
| 9 | **ScheduleFree** | 8.4 | — | candidate |
| 10 | **Lookahead** | 8.4 | **🥇 loss** | candidate |
| 11 | **MARS** | 8.9 | — | candidate |
| 12 | AdaMuon | 10.6 | — | in-house |
| 13 | **Adan** | 11.7 | — | candidate |
| 14 | **AdEMAMix** | 12.0 | — | candidate |
| 15 | **Grams** | 12.0 | — | candidate |
| 16 | **Adai** | 12.0 | — | candidate |
| 17 | **SAM** | 12.9 | — (frontier-mover; mean rank misjudges it — see below) | candidate |

**Two candidates take a rank-1 axis outright: ADOPT (generalization) and Lookahead (loss).** ADOPT is
the best candidate overall (3rd). The in-house Lion/Adakaon/AdaPNM still own the top of the mean rank;
no candidate dethrones them on the average, but several beat them on a specific axis.

### 💪 Strengths & weaknesses (per candidate, at best config)
| candidate | overall | strongest axis | weakest axis | one-line verdict |
|---|---|---|---|---|
| **ADOPT** | 3rd | 🥇 **gap +0.0070** (best of all 17); #2 constant-LR; light 2 B/p; fast | loss 0.0920 (worst — underfits) → poor convergence | the gap/regularizer specialist; underfit is by-design → **real-FID test** |
| **AdaBelief** | 6th | low loss 0.0780 + light 2 B/p + balanced | mid gap +0.0155 | the best-rounded light candidate; solid all-rounder |
| **AdamP** | 7th | loss 0.0747 (#2 of all 17) + good gap +0.0132 + good continuity | slow (16.6 ms / 7.45 ms LoRA — the projection) | great quality, pays in speed; projection ~no-ops on the proxy |
| **ScheduleFree** | 9th | low loss 0.0763 + #3 convergence + light + no-schedule | **gap +0.0185 (13th) & worst-ish continuity** | fast & light, but the gap is its weakness (ironic for constant-LR) |
| **Lookahead** | 10th | 🥇 **loss 0.0718** (best of all 17) + #2 time×quality + fast LoRA | mid gap +0.0188 (14th); heavier 4 B/p (φ buffer) | the loss/convergence specialist; pay 2 B/p for the best loss |
| **MARS** | 11th | low loss 0.0752 (#4) | heavier 4 B/p; slow LoRA 6.73 ms; mid gap | middling — competent loss, ordinary elsewhere |
| **Adan** | 13th | decent gap +0.0133 | heavy 3 B/p; slow (17.5 ms / 12.5 ms LoRA) | weak — the cost isn't repaid |
| **AdEMAMix** | 14th | — (nothing standout on the proxy) | heavy 4 B/p; mid everything | under-served by the short proxy (slow EMA wants long runs) — re-judge long |
| **Grams** | 15th | decent loss 0.0806 | **gap +0.0210 (2nd worst) — memorizes**; worst continuity | weak for the gap thesis (drives loss down, gap up) |
| **Adai** | 16th | low gap +0.0073 (but pure underfit) | heaviest 6 B/p; worst loss 0.117; **LoRA 48 ms (10×)** | clear negative — global-reduction class misfits launch-bound LoRA |
| **SAM** | 17th | **moves the (loss,gap) FRONTIER** (−20% gap vs its base Adakaon) — not captured by field-relative ranks | 2× cost → worst time×quality; mid *absolute* gap | mean rank misjudges it; its value is the *mechanism* → **real-FID test** |

> **Why SAM ranks last but matters most.** The overall is *field-relative*: SAM's gap (+0.0163) is mid-pack
> in absolute terms, and its 2× cost tanks the speed/convergence axes. But the controlled A/B (SAM vs the
> SAME Adakaon, matched init/batch/LR) shows SAM **lowers the gap 14–31%** — a real flat-minima effect.
> The proxy mean rank can't see that; a real-LoRA FID can. Same for ADOPT/Adai: their low gap is the
> underfit kind — only FID separates "flat-minima good" from "underfit bad".

### Per-candidate verdict
| candidate | tuned config (proxy) | te / gap @ C128 | B/p | LoRA ms | verdict |
|---|---|---|---|---|---|
| **ScheduleFree** | lr5e-3, warmup=100, const | 0.0763 / +0.0185 | 2.03 | 5.05 | **KEEP** — 🥇 convergence+time×quality; light; no-schedule fits the constant-LR thesis. |
| **ADOPT** | lr4e-3, clip on | 0.0920 / **+0.0070** | 2.03 | 3.56 | **PROMISING** — 🥇 gap (dethrones AdaPNM), light, fast; underfits → needs a real FID test (regularizer). |
| **AdaBelief-b999** | lr1e-3, β2=0.999 | 0.0780 / +0.0155 | 2.03 | 4.82 | **PROMISING** — light, low loss, gap fixed by high β2 (see lesson). Pareto-decent. |
| AdaBelief (β2=0.95) | lr5e-4 | 0.0742 / +0.0240 | 2.03 | 4.84 | superseded by `-b999` — loss-tuned, gap-worst (the trap). |
| MARS | lr1e-3, γ=0.025 | 0.0752 / +0.0156 | 4.03 | 6.64 | mid-pack; heavier (m + prev-grad). Decent loss, ordinary gap. |
| AdEMAMix | lr5e-4, α=5, no warmup | 0.0792 / +0.0162 | 4.03 | 6.57 | under-served by a short proxy (slow-EMA wants long runs) — re-judge long before discarding. |
| Adan | lr1.5e-3, int8 | 0.0783 / +0.0133 | 3.04 | 12.29 | weakest case: heaviest (3 buffers) + slowest; gap ok but not enough to justify the cost. |
| Grams | lr4e-3 | 0.0806 / +0.0210 | 2.03 | 5.01 | light + decent loss, but **gap-poor at scale** (sign(g) drives loss down → memorizes; looked low-gap on the short proxy then reversed). Not for the gap thesis. |
| AdamP | lr1e-3, wd0.05 | 0.0747 / +0.0132 | 2.03 | 7.51 | good loss + decent gap + good continuity + light, **but slowest per-step** (18.2 ms — the per-channel projection cost). Its signature projection barely fires on the proxy (delta=0 ≡ delta=0.1) → ≈AdamW here; its edge needs scale-invariant weights + long training. Re-judge on a real run. |
| Adai | lr1.6e-2 | 0.1170 / +0.0073 | **6.03** | **47.1** | **clear negative result / NOT a keeper.** Heaviest (fp32 `beta1_prod` running product can't be quantized), worst loss (underfits badly — its low gap is pure underfit), and **catastrophically slow on LoRA (47 ms — 10×)** because its *global* v_mean reduction serializes across the 512 tiny adapter tensors. Structurally misaligned with the memory-efficient + launch-bound-LoRA thesis. Useful finding: the global-reduction optimizer class is a poor fit here. |
| **Lookahead** | lr2.4e-3, k=5 | **0.0718** / +0.0188 | 4.03 | 3.69 | **🥇 LOSS — best held-out loss of all 18** (beats Adakaon-bf16 0.0733). Weight-averaging over Adakaon: a cheap, real loss win at Adakaon speed; mid gap. Cost: +1 φ buffer (~2 B/p over Adakaon). A solid keeper for the *loss* axis; wrap any base. |
| **SAM** | lr1.2e-3, rho0.05 | 0.0783 / +0.0163 | 2.03 | 25.9 | **the frontier-mover.** Gap +0.0163 vs its base Adakaon-bf16's +0.0204 = **−20 % at scale** (smoke: −14–31 % vs matched baseline, 2 seeds) via real flat-minima — the only candidate that moves the (loss,gap) frontier, not slides along it. Cost: honest **2×** per step (no persistent state of its own, ~2 B/p = just Adakaon). Tightens the gap at a small loss cost → **the prime candidate for a real-LoRA FID test** (where flat minima should convert to perceptual quality). |

### The headline tables (excerpts)
**Generalization (by gap — the small-data headline):** ADOPT +0.0070 · AdaPNM +0.0079 · AdamW +0.0103 ·
Adan +0.0133 · Lion +0.0142 · AdaBelief-b999 +0.0155 · MARS +0.0156 · … · AdaBelief(β2.95) +0.0240 (last).
**Convergence/time×quality:** ScheduleFree #1 by a clear margin (in the pre-ADOPT 11-field run: 1125
steps / 15.89 s, vs AdamW 25.48 s, AdaPNM 29 s). ⚠️ ADOPT's underfit raised the common target to
0.0920, compressing the current convergence table — read the gap/loss/memory tables for the clean signal.
**Memory:** candidates at 2.03 B/p (AdaBelief, ScheduleFree, ADOPT) tie Adakaon-bf16; MARS/AdEMAMix 4.03; Adan 3.04 (int8).
**Constant-LR continuity:** AdaPNM #1 (+0.0063), ADOPT #2 (+0.0069) — ADOPT keeps its gap with no schedule (Δ≈0).

## ⚠️ The lesson (loss↔gap trap, re-confirmed at scale)
I first tuned every candidate by **held-out loss on the short C96/N800 proxy**. For AdaBelief that drove
β2→0.95 (lowest loss) — but at full scale that config was the **gap-worst of all 13** (+0.0240). Re-tuning
for the gap (β2=0.999, `AdaBelief-b999`) cut the gap **−35 % to +0.0155** with loss barely moving
(0.0742→0.0780). **The proxy-loss optimum is the gap-pessimum.** This is the profiler's documented
degenerate-pick caveat, now confirmed on the real battery: for the small-data thesis, tune candidates on
the **gap / Pareto**, not loss — and the short proxy's gap is too insensitive to tune on (the spread only
opens at C128/N2000). Final judge remains a **real LoRA + FID/KID** (the proxy ranks objective
overfitting, not perceptual fidelity).

## 🔧 Backend dividend (the real prize — proposals for review, not yet applied)
Seven independent implementations converged on the same findings:
1. **Consolidate the codec read/store helpers.** `_dequant_one/_store_one/_dequant_stacked/_store_stacked`
   are copy-pasted across `adapnm`, `adabelief`, `mars`, `ademamix`, `adan` (and Schedule-Free's z-store).
   Lift a prefix-parameterized version into `_momentum_codec.py` (~120 lines saved per optimizer).
2. **Kill the fp32-aliasing footgun class-wide.** Reading an fp32 momentum buffer via `.float()` returns
   the *same* tensor; an in-place update then corrupts stored state. **5 of 7 candidates hit this.** The
   fix every agent found: route momentum through the codec `ema_one`/`ema_stacked` (they `.clone()` fp32) —
   exactly what `adopt.py`/`adakaon.py` do correctly. Make that the only sanctioned path + add a
   `_backend._fresh_fp32()` helper.
3. **A schedule-free / iterate-averaging MIXIN (genuinely new, highest-value).** Schedule-Free's z/x/y +
   `train()`/`eval()` averaging is optimizer-agnostic (the official ships it as a wrapper around *any* base
   optimizer). A kaon mixin (train/eval swap + ckp1 averaging + z-codec storage) would give **AdaPNM,
   Adakaon, AdaMuon schedule-free variants for free** — and SF already won convergence here.
4. **`blend_batched_(pviews, target, weight, bf16_method)`** so the SF train/eval lerp swap is
   stochastic-rounding/kahan-aware (currently a plain `lerp_` — a minor bf16 fidelity gap).
5. **A shared `_schedulers.py`** for hyperparameter warmups (AdEMAMix's α/β3 half-life warmup; reusable for
   Prodigy D-warmup, AdaMuon ns-warmup).
6. **Batched multi-buffer dequant** for heavy optimizers (Adan's 3 quantized buffers = 3× dequant→requant
   launches/step the backend doesn't amortize).
7. **Document the codec invariant**: `ema_one` expects the update in the param's *original* (not matrixized
   `[R,C]`) shape — an undocumented trap that bit two agents.

Also shipped this campaign: `battery.py` now calls `opt.train()/opt.eval()` around the step/evals when an
optimizer exposes them (guarded — every torch optimizer's run stays bit-identical), so Schedule-Free-style
optimizers are measurable in the battery.

## Findings worth keeping regardless of which candidates ship
- **AdEMAMix's slow EMA m2 is precision-sensitive** (int8 +0.9 %, 4bit +2.4 % held-out vs bf16) — long-horizon
  EMAs want bf16. Suggests a future per-buffer dtype knob (cheap m1 + bf16 m2).
- **Adan int8 momentum is lossless** (te 0.0876 fp ≈ 0.0878 int8) — halves its 3-buffer footprint for free.
- **kozistr ScheduleFreeWrapper has a real bug** (`step**lr` should be `step**r`) — not carried; we used the
  official AdamWScheduleFree closed form.

## A note the proxy keeps teaching us (Grams + ADOPT)
The two sign/normalize-based candidates (Grams, ADOPT) **flip behaviour between the short proxy and the
full-scale battery**: on C96/N800 both looked like low-gap regularizers; at C128/N2000 ADOPT *stayed*
low-gap (it genuinely underfits) while **Grams reversed** into a loss-aggressive memorizer (gap +0.0210).
Lesson reinforced: short-proxy gap/loss does not extrapolate for these mechanisms — only the full-scale
battery (and ultimately FID) decides. Don't trust a candidate's proxy gap.

## Status / next
- All 11 candidates on `integration/candidates-v2`, OFF main, individually committed, every test green
  (~280 new tests; full suite 519). Battery field = 18 optimizers, cached.
- **The four to take to a real LoRA + FID/KID A/B** (each owns a real axis): **ScheduleFree**
  (convergence, light, no-schedule), **ADOPT** (gap champion / regularizer), **Lookahead** (best loss,
  cheap), **SAM** (the frontier-mover — flat minima should convert to perceptual quality; worth the 2×).
  AdaBelief-b999 is a fine light all-rounder. The rest are mid/negative on this evidence.
- **Backend mixin PR** (now specified by two wrappers — ScheduleFree + Lookahead): extract the codec
  full-state buffer, then the WrapsOptimizer/SlowWeights train-eval mixin. Highest-value, separate PR.
- **Battery TODO:** make the convergence target fixed/percentile-based (not "worst optimizer's best")
  so adding an underfitting optimizer can't distort the convergence/time×quality dimension (see the
  caveat at the top).
- **Recommended next:** a real SDXL/Flux LoRA + FID/KID A/B for **ScheduleFree** (convergence/time
  champion, light) and **ADOPT** (gap champion / natural regularizer, light) vs the AdaPNM/Adakaon
  baseline — these two earned it. AdaBelief-b999 is a reasonable third (light, low loss, gap fixed).
- MARS/AdEMAMix: only if pursued — gap-aimed re-tune + a *long* run for AdEMAMix (its slow EMA is
  under-served here). Adan/Grams: weak cases on this evidence.
- **Backend consolidations** (the dividend): handle as a separate PR — they touch shared code. The
  highest-value items are the storage-helper consolidation + the fp32-alias fix (6/7 candidates hit it)
  and the **schedule-free mixin** (would give every kaon optimizer a schedule-free variant, and SF won
  convergence here).
