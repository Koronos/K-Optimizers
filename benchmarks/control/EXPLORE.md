# Optimizer configuration explorer

> Generated by [`explore.py`](explore.py) from [`explore_results.json`](explore_results.json). Settings: `C96_N800_s2`. Each row varies ONE knob; **both loss (held-out `te`) and generalization (`gap` = te − tr) are shown — pick by the axis you care about** (the gap is the small-data fine-tuning signal). ⭐ = best on the axis (by gap). Proxy LRs are ~100x real-training LRs — read the SHAPE and the ×AdamW ratio, not the absolute number.


## AdaPNM

### Warmup
_linear-ramp steps — does warmup help, hurt, or do nothing?_

|  | te (loss) | gap (generalization) |
|---|---|---|
| warmup=0 ⭐ | 0.1076 | +0.0028 |
| warmup=50 | 0.1043 | +0.0076 |
| warmup=100 | 0.1024 | +0.0083 |
| warmup=200 | 0.1026 | +0.0086 |

> **Lowest-gap pick:** **warmup=0**.

